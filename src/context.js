import { createContext } from 'react';

export const TodosContext = createContext(null);
export const SortedTodosContext = createContext(null);
export const FilteredTodosContext = createContext(null);
