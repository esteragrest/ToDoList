export const initialOptionsState = {
	isSorted: false,
	searchValue: '',
};

export const optionsReducer = (state = initialOptionsState, actions) => {
	switch (actions.type) {
		default:
			return state;
	}
};
